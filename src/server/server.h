#ifndef SERVER_H
#define SERVER_H
#endif

// Server handling DNS queries and responses
// Calls request_handler/request_handler.h functions
// to parse the request then proceed to respond 
// with request_handler/response_handler.h